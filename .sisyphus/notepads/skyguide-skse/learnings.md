- 2026-03-28: Added `collectCrosshairTarget()` collector with a single `Game.getCurrentCrosshairRef()` lookup and global try/catch fail-safe returning `null` on collection errors.
- 2026-03-28: Mapped known Skyrim form type ids to readable names, with `FormType_<id>` fallback for unknown types to preserve useful telemetry.
- 2026-03-28: Added `collectEnvironmentState()` collector using per-field try/catch blocks for version-variant APIs (`Weather.getCurrentWeather()`, `Game.getSunPositionX/Y/Z`) and default fallbacks (`0`/`null`) to keep collection resilient.
- 2026-03-28: Added `collectQuestState()` in `src/game-state/collectors/quest-collector.ts` to read hardcoded main quest (`MQ101`) and return `QuestState | null` with a global try/catch fail-safe.
- 2026-03-28: For quest objectives, iterating fixed indices `0..10` and including only displayed objectives (`isObjectiveDisplayed`) keeps payload focused while still exposing completion state (`isObjectiveCompleted`).
- 2026-03-28: In current Skyrim Platform typings, `Game.getQuest` and `quest.getEditorId` required guarded compatibility typing (`Quest` import + narrowed runtime check) to satisfy strict TypeScript while preserving required runtime behavior.
- 2026-03-28: Added `collectInputState()` collector with strict key whitelist (W/A/S/D + Shift/Ctrl/Space), try/catch fail-safe returning empty `keysPressed` on errors, and numeric `DxScanCode`-typed constants to avoid ambient const-enum access issues under `isolatedModules`.
- 2026-03-28: Added `collectNearbyNpcs()` in `src/game-state/collectors/nearby-collector.ts` using the same 9-point multi-scan as `collectEnemies()`, with dedup by `formId`, `CONFIG.maxEnemies` cap, and per-field try/catch fallbacks for potentially unstable Skyrim Platform actor APIs.
