# Decisions

## 2026-03-28 Architecture
- Task 2 (config) was pre-done — config.ts already has all polling fields
- Wave 1 starts with Tasks 1 + 3 in parallel
- All collectors go in src/game-state/collectors/ directory
- Actions go in src/actions/ directory
- Events centralized in src/events.ts
