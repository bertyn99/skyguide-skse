{
  "name": "skyguide-skse",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxlint --fix"
  },
  "dependencies": {
    "@skyrim-platform/skyrim-platform": "*"
  },
  "devDependencies": {
    "oxlint": "^0.16.0",
    "tsdown": "^0.9.0",
    "typescript": "^5.0.0"
  }
}
